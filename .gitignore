# symlinks de build do nix (nix build / nixos-rebuild build)
result
result-*

# PNGs ignorados por padrão (screenshots avulsos).
*.png
# Exceção: wallpaper do lockscreen é asset versionado (declarativo).
!home/lockscreen/*.png