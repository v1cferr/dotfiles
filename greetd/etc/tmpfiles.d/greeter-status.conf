# Diretório legível pelo greeter (usuário `greeter`). O coletor root escreve aqui.
# /run é tmpfs — manter só status.json + ~4 GIFs (wallpaper é fixo em /etc/greetd).
d /run/greeter-status 0755 root root -
