# pkgs/ — derivations próprias ("AUR pessoal")

Reservado (fase 5 do README): pacotes sem equivalente no nixpkgs —
NetExtender (`buildFHSEnv`), AppImages (`appimageTools`), `nix-init`, etc.

Cada derivation vira um `.nix` aqui e é referenciada via overlay no `flake.nix`.
Vazio por ora — placeholder pra segurar a estrutura.
