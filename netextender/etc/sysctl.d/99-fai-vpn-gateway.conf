# Roteamento IPv4 para o "VPN gateway" da FAI: esta máquina (.10) encaminha o
# tráfego da LAN para os subnets da FAI através do túnel snwl_ssltunnel.
# Hoje o Docker já liga isso em runtime; fixamos aqui para não depender dele.
net.ipv4.ip_forward = 1
