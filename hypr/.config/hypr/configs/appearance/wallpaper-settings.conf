#!/bin/bash
############################
### WALLPAPER SETTINGS   ###
############################

# This file contains wallpaper-related settings and configurations
# Source this file in bash scripts for wallpaper management
# DO NOT include this in hyprland.conf - it's for shell scripts only!

# Wallpapers directory
WALLPAPERS_DIR="$HOME/Pictures/Wallpapers"

# Default wallpaper
DEFAULT_WALLPAPER="$WALLPAPERS_DIR/acoolrocket-dalle2-hokusai-non-prompt-landscape.png"

# Supported image formats
SUPPORTED_FORMATS=("jpg" "jpeg" "png" "webp" "bmp")

# Auto-change settings
AUTO_CHANGE_ENABLED=false
AUTO_CHANGE_INTERVAL=300  # 5 minutes in seconds

# Monitor-specific wallpapers (optional)
# Format: MONITOR_NAME="wallpaper_path"
# DP1_WALLPAPER="$WALLPAPERS_DIR/monitor1.png"
# HDMI_A1_WALLPAPER="$WALLPAPERS_DIR/monitor2.png"

# Hyprpaper settings (for reference - actual config is in hyprpaper.conf)
HYPRPAPER_SPLASH=false
HYPRPAPER_IPC=true

# Advanced options
USE_RELOAD_METHOD=true  # Use the new hyprctl hyprpaper reload method (faster)
AVOID_DUPLICATE_WALLPAPERS=true  # Don't set the same wallpaper twice in a row

# Enhanced randomization settings
WALLPAPER_HISTORY_SIZE=20  # Number of recent wallpapers to avoid repeating
RANDOMIZATION_METHOD="smart"  # "smart" (avoid recent) or "pure" (completely random)
CACHE_DIR="$HOME/.cache/hypr"  # Cache directory for wallpaper history
HISTORY_FILE="$CACHE_DIR/wallpaper_history"  # History file path

# Smart selection weights
NEVER_USED_WEIGHT=10  # Weight for wallpapers never used
OLD_WALLPAPER_WEIGHT=5  # Weight for wallpapers not used recently
RECENT_WALLPAPER_WEIGHT=1  # Weight for recently used wallpapers

# Validation settings
VALIDATE_IMAGE_FILES=true  # Check if image files are valid before using
CACHE_VALID_WALLPAPERS=true  # Cache list of valid wallpapers for performance
