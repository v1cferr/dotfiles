# symlinks de build do nix (nix build / nixos-rebuild build)
result
result-*

# PNGs ignorados por padrão (screenshots avulsos). Os wallpapers do lockscreen
# agora vêm do pkgs.nixos-artwork (declarativos), não são mais versionados aqui.
*.png